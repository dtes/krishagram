import React, { Component } from 'react';
import { View, Text } from 'react-native';

class LandFilters extends Component {
  constructor(props) {
    super(props);
    this.state = {
    };
  }

  render() {
    return (
      <View>
        <Text> LandFilters </Text>
      </View>
    );
  }
}

export default LandFilters;
